using System;
using System.Collections.Generic;
using System.Text;

namespace Dapper.Extension.Constants
{
    public class LogicalOperators
    {
        public const String AND = nameof(AND);
        public const String OR = nameof(OR);
        public const String XOR = nameof(XOR);
    }
}
